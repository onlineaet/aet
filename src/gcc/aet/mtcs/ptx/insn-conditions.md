(define_conditions [
  (-1 "!TARGET_SOFT_STACK")
  (-1 "DImode == SImode || TARGET_SM35")
  (-1 "TARGET_PTX_6_0")
  (-1 "Pmode == DImode")
  (1 "SImode == SImode || TARGET_SM35")
  (-1 "TARGET_SM35")
  (-1 "Pmode == SImode")
  (1 " 1")
  (-1 "(TARGET_PTX_7_3
   && TARGET_SM52) && (Pmode == SImode)")
  (-1 "(TARGET_SOFT_STACK) && (Pmode == SImode)")
  (-1 "TARGET_SM53")
  (-1 "can_create_pseudo_p ()")
  (-1 "TARGET_SM80")
  (-1 "!TARGET_SM70")
  (-1 "flag_unsafe_math_optimizations")
  (-1 "TARGET_SM70")
  (-1 "TARGET_SM75 && flag_unsafe_math_optimizations")
  (-1 "(TARGET_SOFT_STACK) && (Pmode == DImode)")
  (-1 "(TARGET_PTX_7_3
   && TARGET_SM52) && (Pmode == DImode)")
  (-1 "!MEM_P (operands[0]) || REG_P (operands[1])")
  (-1 "CONST_DOUBLE_P (operands[2])
   && real_identical (CONST_DOUBLE_REAL_VALUE (operands[2]), &dconst1)")
])
